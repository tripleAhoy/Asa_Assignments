#28

import statistics

values = [9, 11,17,22,22,22,34,34,34,40]
	 

print('the mean value is:', statistics.mean(values))
print('the mode value is:', statistics.mode(values))
print('the median value is:', statistics.median(values))
